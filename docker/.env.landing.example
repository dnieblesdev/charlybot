# Landing SSR — Development Environment Variables
# Copy to .env.landing and fill in values
# Use: docker compose -f docker/docker-compose.dev.yml up

# SSR runtime port (Angular dev server)
PORT=4200
