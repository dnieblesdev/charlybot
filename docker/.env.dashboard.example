# Dashboard — Development Environment Variables
# Copy to .env.dashboard (currently empty — no runtime env vars needed)
# API calls are proxied through nginx at /api/v1/*
# Use: docker compose -f docker/docker-compose.dev.yml up
