# Valkey — Development Environment Variables
# Copy to .env.valkey and fill in real values
# Use: docker compose -f docker/docker-compose.dev.yml up

# Valkey authentication (optional but recommended)
# Leave empty or remove to disable password auth in dev
VALKEY_PASSWORD=your_valkey_password