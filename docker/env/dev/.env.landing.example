# Landing SSR — Development Environment Variables
# Copy to .env.landing and fill in values if needed
# Use: docker compose -f docker/docker-compose.dev.yml up

# SSR server port
PORT=4200

# No other env vars required for landing page