# Valkey — Production Environment Variables
# Copy to .env.valkey and fill in real values
# Use: docker compose -f docker/docker-compose.yml up

# Valkey authentication (strongly recommended for production)
VALKEY_PASSWORD=your_secure_valkey_password