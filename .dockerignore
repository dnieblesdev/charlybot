# Dependencies
node_modules

# Output
dist
build

# Git
.git
.gitignore

# IDE
.vscode
.idea

# Docs
*.md
!package.json

# Docker (excluir del copy)
# NOTE: Necesitamos incluir el entrypoint de runtime install en el build context.
# Por eso ignoramos el directorio docker por defecto, pero re-incluimos lo necesario.
docker/*
!docker/docker/
!docker/docker/entrypoint.sh

# Database (excluir del copy, se monta como volumen)
*.db
*.db-journal

# Logs
logs
*.log

# Test
coverage

# Temp
tmp
temp
*.tmp
