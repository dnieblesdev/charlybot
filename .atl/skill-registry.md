# Skill Registry — charlybot

<!-- Auto-generated by gentle-ai skill-registry refresh. Run `gentle-ai skill-registry refresh --force` to regenerate. -->

Last updated: 2026-05-15

## Sources scanned

- skills
- C:\Users\dnieb\.config\opencode\skills
- C:\Users\dnieb\.codex\skills

## Contract

**Delegator use only.** Any agent that launches subagents reads this registry to resolve compact rules, then injects matching rule text into subagent prompts under `## Project Standards (auto-resolved)`.

Subagents still read their assigned executor/phase skill. During normal runtime, they do **not** independently discover or load additional project/user `SKILL.md` files or this registry; project/user rules arrive pre-digested. Explicit fallback loading is degraded self-healing and must be reported in `skill_resolution` as `fallback-registry` or `fallback-path`.

## Selected skills and compact rules

### accessibility
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\accessibility\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### adev-writing-guide
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\adev-writing-guide\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### ai-sdk-5
- Path: C:\Users\dnieb\.config\opencode\skills\ai-sdk-5\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### angular-developer
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\angular-developer\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### architecture
- Path: C:\Users\dnieb\.config\opencode\skills\angular\architecture\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### bash-defensive-patterns
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\bash-defensive-patterns\SKILL.md
- Trigger: Master defensive Bash programming techniques for production-grade scripts.
- Rules:
  - Start every script with `set -Eeuo pipefail`
  - Use `trap` for cleanup on EXIT and error reporting on ERR
  - Quote all variable expansions: `"$var"` not `$var`
  - Use `mktemp` for temp files, never hardcoded paths
  - Check command success with `if ! command; then` not `$?`
  - Prefer `[[ ]]` over `[ ]` for conditionals
  - Run ShellCheck on all scripts before committing

### branch-pr
- Path: C:\Users\dnieb\.config\opencode\skills\branch-pr\SKILL.md
- Trigger: Create Gentle AI pull requests with issue-first checks. Trigger: creating, opening, or preparing PRs for review.
- Rules:
  - **Every PR MUST link an approved issue** — no exceptions
  - **Every PR MUST have exactly one `type:*` label**
  - **Automated checks must pass** before merge is possible
  - **Blank PRs without issue linkage will be blocked** by GitHub Actions

### bun
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\bun\SKILL.md
- **DEPRECATED** — Project migrated to pnpm + Node.js 22. This skill is retained for reference only.
- Rules:
  - Do NOT use bun for any new work. Use pnpm for package management, Node.js 22 for runtime, tsx for TypeScript execution.

### chained-pr
- Path: C:\Users\dnieb\.config\opencode\skills\chained-pr\SKILL.md
- Trigger: Trigger: PRs over 400 lines, stacked PRs, review slices. Split oversized changes into chained PRs that protect review focus.
- Rules:
  - Split PRs over **400 changed lines** unless a maintainer explicitly accepts `size:exception`.
  - Keep each PR reviewable in about **≤60 minutes**.
  - Use one deliverable work unit per PR; keep tests/docs with the unit they verify.
  - State start, end, prior dependencies, follow-up work, and out-of-scope items in every chained PR.
  - Every child PR must include a dependency diagram marking the current PR with `📍`.
  - In Feature Branch Chain, create a draft/no-merge tracker PR; child PR #1 targets the tracker branch, later children target the immediate parent branch.
  - Treat polluted diffs as base bugs: retarget or rebase until only the current work unit appears.
  - Do not mix chain strategies after the user chooses one.
  - Condition: Action
  - PR ≤400 changed lines and focused: Keep single PR.
  - PR >400, each slice can land independently: Use Stacked PRs to main.
  - PR >400, feature must integrate before main: Use Feature Branch Chain with tracker.
  - Generated/vendor/migration diff cannot split cleanly: Ask maintainer for `size:exception`.
  - SDD provides `delivery_strategy`: Follow it before apply/PR creation.

### changeset-workflow
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\changeset-workflow\SKILL.md
- Rules:
  - Rule: Why
  - Use `pnpm exec changeset add --empty` (never interactive `pnpm exec changeset add` without `--empty`): The interactive CLI hangs with AI agents
  - List `.changeset/` after `--empty` to find the generated file: File names are random: `adjective-noun-verb.md`
  - Quote scoped package names in YAML: `"@charlybot/bot"`: YAML requires quotes for `@`-prefixed keys
  - Run `pnpm exec changeset version` AFTER all changesets are committed: This bumps package.json versions and updates CHANGELOGs
  - Commit release with `chore(release): ...` message: Conventional commit standard for releases
  - Rule: Consequence
  - Run `pnpm exec changeset add` without `--empty`: Interactive prompts hang the agent indefinitely
  - Forget to commit changeset `.md` files before `pnpm exec changeset version`: Version command won't find uncommitted changesets
  - Push branches other than `master`: `develop` stays local/CI; only `master` goes to remote
  - Use `changeset` CLI for the version commit message: Use `git commit -m "chore(release): ..."` manually
  - Description of changes
  - Bullet points for changelog

### cognitive-doc-design
- Path: C:\Users\dnieb\.config\opencode\skills\cognitive-doc-design\SKILL.md
- Trigger: Design docs that reduce cognitive load. Trigger: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs.
- Rules:
  - Pattern: Rule
  - Lead with the answer: Put the decision, action, or outcome first. Context comes after.
  - Progressive disclosure: Start with the happy path, then add details, edge cases, and references.
  - Chunking: Group related information into small sections. Keep flat lists short.
  - Signposting: Use headings, labels, callouts, and summaries so readers know where they are.
  - Recognition over recall: Prefer tables, checklists, examples, and templates over prose that must be remembered.
  - Review empathy: Design docs so reviewers can verify intent without reconstructing the whole story.

### comment-writer
- Path: C:\Users\dnieb\.config\opencode\skills\comment-writer\SKILL.md
- Trigger: Write warm, direct collaboration comments. Trigger: PR feedback, issue replies, reviews, Slack messages, or GitHub comments.
- Rules:
  - Be useful fast: Start with the actionable point. Do not recap the whole PR before feedback.
  - Be warm and direct: Sound like a thoughtful teammate, not a corporate bot.
  - Keep it short: Prefer 1 to 3 short paragraphs or a tight bullet list.
  - Explain why: Give the technical reason when asking for a change.
  - Avoid pile-ons: Comment on the highest-value issue, not every tiny preference.
  - Match thread language: Write in the thread/user language. If writing in Spanish, use Rioplatense Spanish/voseo: `podés`, `tenés`, `fijate`, `dale`.
  - No em dashes: Use commas, periods, or parentheses instead.

### core
- Path: C:\Users\dnieb\.config\opencode\skills\angular\core\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### design-md
- Path: C:\Users\dnieb\.config\opencode\skills\design-md\SKILL.md
- Trigger: >
- Rules:
  - #: Rule
  - 1: YAML front matter delimited by `---` on its own lines
  - 2: Colors = `#` + hex (6 or 8 digits, sRGB). NO `rgb()`, `hsl()`, color names.
  - 3: `fontWeight` = number (e.g., `400`), not string `"400"`
  - 4: `fontSize`, `letterSpacing` = Dimension (number + `px`/`em`/`rem`)
  - 5: `lineHeight` = Dimension OR unitless number (e.g., `1.6`)
  - 6: Token references = `{path.to.token}` syntax
  - 7: Section order: Overview → Colors → Typography → Layout → Elevation → Shapes → Components → Do's and Don'ts
  - 8: Duplicate section headings = ERROR. Merge, don't duplicate.
  - 9: Unknown sections/tokens/values = ACCEPT. Do not strip.
  - Canonical name: Also accepts
  - Overview: Brand & Style
  - Layout: Layout & Spacing
  - Elevation & Depth: Elevation
  - Property: Type

### discord-command
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\discord-command\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### django-drf
- Path: C:\Users\dnieb\.config\opencode\skills\django-drf\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### forms
- Path: C:\Users\dnieb\.config\opencode\skills\angular\forms\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### frontend-design
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\frontend-design\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### go-testing
- Path: C:\Users\dnieb\.config\opencode\skills\go-testing\SKILL.md
- Trigger: Trigger: Go tests, go test coverage, Bubbletea teatest, golden files. Apply focused Go testing patterns.
- Rules:
  - Prefer table-driven tests for multiple cases; use `t.Run(tt.name, ...)`.
  - Test behavior and state transitions, not implementation trivia.
  - Use `t.TempDir()` for filesystem tests; never rely on a real home directory.
  - Keep integration tests skippable with `testing.Short()` when they run external commands or slow flows.
  - For Bubbletea, test `Model.Update()` directly for state changes; use `teatest` only for interactive flows.
  - Golden files must be deterministic; update only through the repo's `-update` path and rerun tests without `-update`.
  - Use small mocks/interfaces around system or command execution boundaries.
  - Pure function or parser: Table-driven unit test.
  - Error behavior: Explicit success and failure cases.
  - File operations: `t.TempDir()` plus focused assertions.
  - TUI state transition: Direct `Model.Update()` call with `tea.Msg`.
  - Full TUI interaction: `teatest.NewTestModel()`.
  - Rendered output: Golden file test.
  - Real external command: Integration test; skip in `-short`.

### hono
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\hono\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### imagegen
- Path: C:\Users\dnieb\.codex\skills\.system\imagegen\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### issue-creation
- Path: C:\Users\dnieb\.config\opencode\skills\issue-creation\SKILL.md
- Trigger: Create Gentle AI issues with issue-first checks. Trigger: creating GitHub issues, bug reports, or feature requests.
- Rules:
  - **Blank issues are disabled** — MUST use a template (bug report or feature request)
  - **Every issue gets `status:needs-review` automatically** on creation
  - **A maintainer MUST add `status:approved`** before any PR can be opened
  - **Questions go to [Discussions](https://github.com/Gentleman-Programming/agent-teams-lite/discussions)**, not issues

### jira-epic
- Path: C:\Users\dnieb\.config\opencode\skills\jira-epic\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### jira-task
- Path: C:\Users\dnieb\.config\opencode\skills\jira-task\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### judgment-day
- Path: C:\Users\dnieb\.config\opencode\skills\judgment-day\SKILL.md
- Trigger: Trigger: judgment day, dual review, adversarial review, juzgar. Run blind dual review, fix confirmed issues, then re-judge.
- Rules:
  - Resolve project skills before launching agents: read skill registry, match compact rules by target files/task, and inject the same `Project Standards` block into both judge prompts and fix prompts.
  - Launch **two blind judges in parallel** with identical target and criteria; never review the code yourself.
  - Wait for both judges before synthesis; never accept a partial verdict.
  - Classify warnings as `WARNING (real)` only if normal intended use can trigger them; otherwise downgrade to INFO as `WARNING (theoretical)`.
  - Ask before fixing Round 1 confirmed issues.
  - After any fix agent runs, immediately re-launch both judges in parallel before commit/push/done/session summary.
  - Terminal states are only `JUDGMENT: APPROVED` or `JUDGMENT: ESCALATED`.
  - After 2 fix iterations with remaining issues, ask the user whether to continue.
  - Condition: Action
  - Target unclear: Ask for scope; do not launch judges.
  - No skill registry: Warn, proceed with generic criteria, and record `Skill Resolution: none`.
  - Both judges find same CRITICAL/real WARNING: Confirmed; ask/fix according to round rules.
  - One judge finds issue: Suspect; report and triage, do not auto-fix.
  - Judges contradict: Escalate for manual decision.
  - Round 2+ has only theoretical warnings/suggestions: Report as INFO; do not re-judge.

### monorepo-versioning
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\monorepo-versioning\SKILL.md
- Rules:
  - `bun changeset add`
  - `bun changeset version`
  - `changeset publish`
  - archivos `.changeset/*.md`
  - `apps/*`
  - `packages/*`
  - `package.json` raíz
  - `apps/*/package.json`
  - `packages/*/package.json`
  - La instalación se gestiona desde la **raíz del monorepo**
  - No asumir `node_modules` por app como mecanismo de release
  - El flujo de versionado es **independiente** del package manager
  - Detectar workspaces modificados
  - Construir grafo de dependencias internas
  - Calcular workspaces impactados indirectamente

### nextjs-15
- Path: C:\Users\dnieb\.config\opencode\skills\nextjs-15\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### nodejs-backend-patterns
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\nodejs-backend-patterns\SKILL.md
- Trigger: Build production-ready Node.js backend services.
- Rules:
  - Use structured error handling with typed error classes (AppError hierarchy)
  - Apply security middleware: helmet, cors, rate limiting
  - Async handlers MUST have try/catch or use express-async-errors wrapper
  - Validate all input at the boundary (Zod schemas before business logic)
  - Use dependency injection for testability (not hardcoded imports)
  - Structured logging with Winston, never console.log
  - Health check endpoint for orchestration readiness

### nodejs-best-practices
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\nodejs-best-practices\SKILL.md
- Trigger: Node.js development principles and decision-making.
- Rules:
  - Choose framework by context: Hono for edge/serverless, Fastify for perf, Express for stability
  - Always use async/await, never raw callbacks or .then() chains without error handling
  - Validate all external input at system boundaries
  - Never block the event loop — offload CPU-heavy work to worker threads
  - Use environment variables for config, never hardcode secrets
  - Graceful shutdown: handle SIGTERM, close DB connections, drain requests

### nodejs-express-server
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\nodejs-express-server\SKILL.md
- Trigger: Build production-ready Express.js servers.
- Rules:
  - Use ESM imports (import/export), never require() in TypeScript files
  - Middleware order matters: security → parsing → routes → error handler
  - Always return proper HTTP status codes with consistent JSON shape
  - Separate route handlers from business logic (thin routes, fat services)
  - Use error-handling middleware (4-param) as the final middleware
  - Set security headers: helmet, CORS, rate limiting

### openai-docs
- Path: C:\Users\dnieb\.codex\skills\.system\openai-docs\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### performance
- Path: C:\Users\dnieb\.config\opencode\skills\angular\performance\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### playwright
- Path: C:\Users\dnieb\.config\opencode\skills\playwright\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### plugin-creator
- Path: C:\Users\dnieb\.codex\skills\.system\plugin-creator\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### pnpm
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\pnpm\SKILL.md
- Rules:
  - Use `pnpm-workspace.yaml` for monorepo config (packages: ['apps/*', 'packages/*'])
  - Always `--frozen-lockfile` in CI/Docker; commit `pnpm-lock.yaml`
  - Use `pnpm exec` (not npx/bunx) for package binaries
  - `workspace:*` protocol for internal dependencies
  - `--filter` for scoped operations (`pnpm --filter @charlybot/bot test`)
  - Strict mode: undeclared transitive deps WILL fail to import — declare them explicitly
  - `node-linker=hoisted` in `.npmrc` for native C++ addons (sodium-native, opus)
  - Corepack enabled: `packageManager` field in root package.json

### pr-review
- Path: C:\Users\dnieb\.config\opencode\skills\pr-review\SKILL.md
- Rules:
  - [ ] Test/debug files committed (`test.js`, `console.log`, etc.)
  - [ ] Variables declared but never used
  - [ ] Code duplication instead of refactoring
  - [ ] Broken indentation or syntax errors
  - [ ] Config files with personal/local settings
  - [ ] Hardcoded secrets or credentials
  - [ ] Breaking changes without migration path
  - [ ] Too many commits (should squash)
  - [ ] Missing validation for dependencies (e.g., `jq`, `curl`)
  - [ ] Potential conflicts with other PRs
  - [ ] Incomplete feature (e.g., variable declared but not used)
  - [ ] Fallback code with bugs (e.g., not escaping newlines)
  - [x] Small, focused changes
  - [x] Tests included
  - [x] Clean commit history (1-3 commits)

### pr_review
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\pr_review\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### prisma-cli
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\prisma-cli\SKILL.md
- Rules:
  - Use `pnpm exec prisma` for all Prisma CLI commands (NOT bunx --bun)
  - `pnpm exec prisma generate` for client regeneration
  - `pnpm exec prisma db push` for prototyping
  - `pnpm exec prisma migrate dev` for migration creation
  - Schema path: `--schema=./packages/shared/prisma/schema.prisma` (run from repo root)

### prisma-client-api
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\prisma-client-api\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### prisma-database-setup
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\prisma-database-setup\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### prisma-postgres
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\prisma-postgres\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### pytest
- Path: C:\Users\dnieb\.config\opencode\skills\pytest\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### react-19
- Path: C:\Users\dnieb\.config\opencode\skills\react-19\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### reference-compiler-cli
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\reference-compiler-cli\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### reference-core
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\reference-core\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### reference-signal-forms
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\reference-signal-forms\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### seo
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\seo\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### skill-creator
- Path: C:\Users\dnieb\.config\opencode\skills\skill-creator\SKILL.md
- Trigger: Trigger: new skills, agent instructions, documenting AI usage patterns. Create LLM-first skills with valid frontmatter.
- Rules:
  - When working in this repo, first follow `docs/skill-style-guide.md` as the normative source before creating or updating skills.
  - If that guide is unavailable, use the compact inline rules below.
  - A skill is a runtime instruction contract for an LLM, not human documentation.
  - Do not add a `Keywords` section; preserve essential trigger words in `description`.
  - References must point to local files.
  - Keep the skill body concise: target 180–450 tokens, recommended max 700, hard max 1000.
  - Need: Action
  - Code templates, schemas, fixtures, generated examples: Put them in `assets/`
  - Conceptual detail, edge cases, existing docs: Put local links in `references/`
  - Long explanation in `SKILL.md`: Move it to a supporting file
  - Multiple meaningful paths: Add a compact decision table

### skill-installer
- Path: C:\Users\dnieb\.codex\skills\.system\skill-installer\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### skill-style-guide
- Path: C:\Users\dnieb\.config\opencode\skills\skill-style-guide\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### tailwind-4
- Path: C:\Users\dnieb\.config\opencode\skills\tailwind-4\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### tailwind-css-patterns
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\tailwind-css-patterns\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### typescript
- Path: C:\Users\dnieb\.config\opencode\skills\typescript\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### typescript-advanced-types
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\typescript-advanced-types\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### vite
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\vite\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### vitest
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\vitest\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### work-unit-commits
- Path: C:\Users\dnieb\.config\opencode\skills\work-unit-commits\SKILL.md
- Trigger: Plan commits as reviewable work units. Trigger: implementation, commit splitting, chained PRs, or keeping tests and docs with code.
- Rules:
  - Commit by work unit: A commit represents a deliverable behavior, fix, migration, or docs unit.
  - Do not commit by file type: Avoid `models`, then `services`, then `tests` if none works alone.
  - Keep tests with code: Tests belong in the same commit as the behavior they verify.
  - Keep docs with the user-visible change: Docs belong with the feature or workflow they explain.
  - Tell a story: A reviewer should understand why each commit exists from its diff and message.
  - Future PR-ready: Each commit should be a candidate chained PR when the change grows.
  - SDD workload guard: If SDD tasks forecast a >400-line change, group commits into chained PR slices before implementation.

### zod
- Path: F:\Vault\20_Desarrollo\20.10_Activos\charlybot\skills\zod\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### zod-4
- Path: C:\Users\dnieb\.config\opencode\skills\zod-4\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### zustand-5
- Path: C:\Users\dnieb\.config\opencode\skills\zustand-5\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.
